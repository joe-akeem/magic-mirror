# magic-mirror

## Training

### Prepare Training Data

### Adding Training data

Add new subjects to the folder training_data
In the project root directory: call `python create_csv.py training_data/ > training_data.csv`

### Train
